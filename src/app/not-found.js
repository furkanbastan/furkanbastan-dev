export default function NotFound() {
    const style = {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    };
    return <div style={style}>404 | Page Nout Found</div>;
}
